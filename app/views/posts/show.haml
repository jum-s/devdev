.row
  .jump
  .medium-7.medium-centered.columns
    .reading_box
      %br 
      %h3= @post.title.html_safe
      %p.source= @post.created_at.strftime("%e %B %Y")
      %p= @post.text.html_safe 
      %br
    %br
    = link_to(t('home'), root_path, class: "small button radius")
    = link_to(t('food'), posts_path, class: "small button radius")
